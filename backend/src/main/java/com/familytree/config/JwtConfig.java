package com.familytree.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // JWT configuration is handled via @Value in JwtTokenProvider
    // This class could be used for additional bean definitions if needed.
}
